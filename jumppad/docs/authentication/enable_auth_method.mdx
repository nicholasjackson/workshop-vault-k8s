# Enabling the Kubernetes Auth Method

All secret engines and authentication methods in Vault are disabled by default.
To enable an authentication method, use the `vault auth enable` command followed by the type of authentication method you want to enable.


<Task id="configure_auth">

Let's enable the Kubernetes authentication method in Vault by running the following command in your terminal:

```bash
vault auth enable kubernetes
```

</Task>